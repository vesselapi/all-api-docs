---
title: "Get Event Attendee Details"
api: "GET /crm/event/attendee/details"
description: "Get details about all event attendees."
---

Details include the type, possible values, and other metadata about the fields.

### Parameters

<Param query="accessToken" type="string" required>

The token for the customer's CRM account. This was generated when they connected their account.

</Param>

<Param query="allFields" type="boolean">

Returns details about non-universal fields

</Param>

<Param query="cursor" type="string"></Param>

<Param query="id" type="string">
  If provided, get the details about a particular deal. If not provided, get
  details about all deals.
</Param>

### Response

<ResponseField name="fields" type="Field[]">

<Expandable title="Field">
  <ResponseField name="key" type="string" required>
    The key in the CRM object (ex: annualRevenue, numberOfEmployees, etc)
  </ResponseField>
  <ResponseField name="name" type="string" required>
    The display name of this field (ex: number of employees, annual revenue, etc)
  </ResponseField>
  <ResponseField name="type" type="string" required>
    See Field model for possible values
  </ResponseField>
  <ResponseField name="options" type="object[]" required>
  The key in the CRM object (ex: annualRevenue, numberOfEmployees, etc)

  <Expandable title="object">
    <ResponseField name="key" type="string"></ResponseField>
    <ResponseField name="name" type="string"></ResponseField>
  </Expandable>
  </ResponseField>

  <ResponseField name="isArray" type="boolean">
    If this field is an array
  </ResponseField>
  <ResponseField name="universal" type="boolean" required>
    If this is a field we've unified across CRMs
  </ResponseField>
  <ResponseField name="creatable" type="boolean" required>
    If this field can be used when creating the object
  </ResponseField>
  <ResponseField name="updatable" type="boolean" required>
    If this field can be updated
  </ResponseField>
</Expandable>

</ResponseField>

<ResponseField name="nextPageCursor" type="string"></ResponseField>

<RequestExample>

```bash Request
curl --request GET \
  --url https://api.vessel.land/crm/event/attendee/details \
  --header 'Content-Type: application/json' \
  --header 'vessel-api-token: '
```

</RequestExample>

<ResponseExample>

```json Response Example
{
  "fields": [
    {
      "key": "email",
      "name": "Email",
      "type": "email",
      "options": null,
      "isArray": false,
      "custom": false,
      "universal": true,
      "creatable": true,
      "updatable": true
    }
  ],
  "nextPageCursor": "string"
}
```

</ResponseExample>
