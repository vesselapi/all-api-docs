---
openapi: "PATCH /crm/lead/custom-field"
---

<RequestExample>

```bash Request
curl --request PATCH \
  --url https://api.vessel.land/crm/lead/custom-field \
  --header 'Content-Type: application/json' \
  --header 'vessel-api-token: ' \
  --data '{
  "accessToken": "string",
  "customField": {
    "key": "optional_key_for_custom_field",
    "options": [
      {
        "name": "First Name",
        "key": "first_key"
      },
      {
        "name": "Second Name",
        "key": "second_key"
      }
    ]
  }
}'
```

</RequestExample>

**CRM Caveats**  
Supported for HubSpot and Salesforce single and mutli-select fields.
